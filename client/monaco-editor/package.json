{
  "name": "@opencodegraph/monaco-editor-extension",
  "version": "0.0.3",
  "description": "OpenCodeGraph extension for Monaco Editor",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/sourcegraph/opencodegraph",
    "directory": "client/monaco-editor"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist",
    "!**/*.test.*"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc --build",
    "test": "vitest",
    "demo": "pnpm -C demo run -s dev"
  },
  "dependencies": {
    "@opencodegraph/client": "workspace:*"
  },
  "devDependencies": {
    "monaco-editor": "^0.45.0"
  },
  "peerDependencies": {
    "monaco-editor": "^0.45"
  }
}
