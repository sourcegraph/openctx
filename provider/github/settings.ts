export type Settings = {
    /** The accessToken to use to talk to the GitHub API. */
    accessToken: string
    /** Optional base URL of a GitHub enterprise instance, when not using GitHub.com. */
    gheURL?: string
}
