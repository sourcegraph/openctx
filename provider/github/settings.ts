export type Settings = {
    /** The accessToken to use to talk to the GitHub API. */
    accessToken: string
    /** Optional URL of the GitHub instance, when not using GitHub.com. */
    baseURL?: string
}
